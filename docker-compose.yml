version: '3.8'

services:
  multiplayer-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SESSION_LIMIT=10
    restart: unless-stopped
    volumes:
      - ./test-client.html:/app/test-client.html